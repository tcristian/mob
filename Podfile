# Uncomment the next line to define a global platform for your project
# platform :ios, ’10.0’

target 'mdb' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mdb
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Alamofire', '~> 4.4'

  target 'mdbTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mdbUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
